# docs/misc-updates — Miscellaneous notes and repo ergonomics

Small operational notes and repository-level changes made during this work:

- Commit template: `.gitmessage.txt` (repo root) and local git config set to use it.
- VS Code: `.vscode/settings.json` updated to open commit messages in the editor.
- Local CLI tooling: AWS CLI v2 and SAM CLI installed locally (see top-level README for versions used).

If you'd like, I can move the root `docs/*.md` files into these folders and update any cross-references. For now, these index files make the grouping explicit without renaming files.
